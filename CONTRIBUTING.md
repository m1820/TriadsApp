# Contributing to Triad Shapes PWA

1. Fork the repo
2. Create branch (`git checkout -b feature/add-chord`)
3. Commit (`git commit -m "Add new chord shape"`)
4. Push (`git push origin feature/add-chord`)
5. Open PR

Follow ESLint. Test on mobile.